[tool.ruff]
target-version = "py310"
line-length = 100

# Keep this intentionally conservative so existing code doesn't need a big refactor.
[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"


